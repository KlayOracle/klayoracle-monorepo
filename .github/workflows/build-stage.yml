name: 'Build Node & Data Provider for Staging'

on:
  pull_request:
  push:
    branches:
      - staging

jobs:
  data-provider-unit-test:
    runs-on: ubuntu-18.04
    steps:
      - name: Build
        run: echo Build!
