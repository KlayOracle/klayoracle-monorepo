name: Slither analysis
on: [push]
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: View directory
        run: ls -la oracle-contract
#      - uses: crytic/slither-action@v0.2.0
#        with:
#          node-version: 18
#          target: 'oracle-contract/'