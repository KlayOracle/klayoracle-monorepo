import type * as nt from 'nekoton-wasm';
import { Address } from 'everscale-inpage-provider';
import { Account, PrepareMessageParams, AccountsStorageContext } from './';
/**
 * @category AccountsStorage
 */
export type GiverVersion = 2 | 3;
/**
 * Any account which supports Giver ABI (GiverV2, GiverV3):
 *
 * ```
 * {
 *   "ABI version": 2,
 *   "header": ["pubkey", "time", "expire"],
 *   "functions": [{
 *     "name": "sendTransaction",
 *     "inputs": [
 *       {"name":"dest","type":"address"},
 *       {"name":"value","type":"uint128"},
 *       {"name":"bounce","type":"bool"},
 *     ],
 *     "outputs": []
 *   }],
 *   "events": []
 * }
 * ```
 *
 * @category AccountsStorage
 */
export declare class GiverAccount implements Account {
    readonly address: Address;
    private readonly publicKey;
    static readonly GIVER_KEY_PAIR: nt.Ed25519KeyPair;
    static fromVersion(version: GiverVersion): GiverAccount;
    constructor(args: {
        address: string | Address;
        publicKey: string;
    });
    fetchPublicKey(_ctx: AccountsStorageContext): Promise<string>;
    prepareMessage(args: PrepareMessageParams, ctx: AccountsStorageContext): Promise<nt.SignedMessage>;
}
