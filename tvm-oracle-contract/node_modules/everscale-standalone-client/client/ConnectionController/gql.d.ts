import type * as nt from 'nekoton-wasm';
/**
 * @category Client
 */
export type GqlSocketParams = {
    /**
     * Path to graphql api endpoints, e.g. `https://mainnet.evercloud.dev/123123/graphql`
     */
    endpoints: string[];
    /**
     * Gql node type
     *
     * @defaultValue `false`
     */
    local?: boolean;
    /**
     * Frequency of sync latency detection
     *
     * @defaultValue `60000`
     */
    latencyDetectionInterval?: number;
    /**
     * Maximum value for the endpoint's blockchain data sync latency
     */
    maxLatency?: number;
};
export declare class GqlSocket {
    connect(clock: nt.ClockWithOffset, params: GqlSocketParams): Promise<nt.GqlConnection>;
    static checkLatency(endpoint: Endpoint): Promise<number | undefined>;
    static expandAddress: (baseUrl: string) => Endpoint;
}
type Endpoint = {
    url: string;
    agent?: any;
};
export {};
