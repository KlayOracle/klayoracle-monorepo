import type * as nt from 'nekoton-wasm';
/**
 * @category Client
 */
export type JrpcSocketParams = {
    /**
     * Full JRPC API endpoint
     */
    endpoint: string;
    /**
     * Alternative JRPC API that will be used for broadcasting messages or fetching states
     */
    alternativeEndpoint?: string;
};
export declare class JrpcSocket {
    connect(clock: nt.ClockWithOffset, params: JrpcSocketParams): Promise<nt.JrpcConnection>;
}
