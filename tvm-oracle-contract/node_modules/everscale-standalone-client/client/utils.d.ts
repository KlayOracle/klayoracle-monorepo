import { Address } from 'everscale-inpage-provider';
import { EventEmitter } from 'events';
export declare class SafeEventEmitter extends EventEmitter {
    emit(type: string, ...args: any[]): boolean;
}
export declare function convertToAddressObject(address: string | Address): Address;
/**
 * @category Utils
 */
export declare const convertVersionToInt32: (version: string) => number;
export declare function getUniqueId(): number;
