import { LockliftConfig } from "../../config";
export type BuilderConfig = {
    includesPath?: string;
    compilerPath: string;
    linkerLibPath: string;
    linkerPath: string;
    externalContracts: LockliftConfig["compiler"]["externalContracts"];
};
type Option = {
    build: string;
    disableIncludePath: boolean;
    contracts: string;
};
export declare class Builder {
    private readonly config;
    private options;
    private nameRegex;
    private docRegex;
    constructor(config: BuilderConfig, options: Option);
    buildContracts(): Promise<boolean>;
    buildDocs(): boolean;
    private parseDocs;
    private getContractsTree;
}
export {};
