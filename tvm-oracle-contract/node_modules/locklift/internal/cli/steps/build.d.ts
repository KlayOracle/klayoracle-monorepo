import { ConfigState, LockliftConfig } from "../../config";
export declare const buildStep: (config: LockliftConfig<ConfigState.INTERNAL>, options: {
    build: string;
    contracts: string;
    disableIncludePath: boolean;
} & Pick<LockliftConfig, "compiler" | "linker">) => Promise<void>;
