import { Locklift, LockliftConfig } from "../../../index";
import { ConfigState } from "../../config";
export declare const initLockliftStep: (config: LockliftConfig<ConfigState.INTERNAL>, options: {
    network?: string;
    script: string;
}) => Promise<Locklift<any>>;
