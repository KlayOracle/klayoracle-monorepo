import { ConfigState, NetworkValue } from "../config";
export declare class LockliftContext {
    readonly network: {
        name: string;
        config: NetworkValue<ConfigState.INTERNAL>;
    };
    constructor(network: {
        name: string;
        config: NetworkValue<ConfigState.INTERNAL>;
    });
}
