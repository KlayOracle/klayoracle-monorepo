export declare class SeService {
    private readonly rpcUrl;
    constructor(rpcUrl: string);
    getCurrentOffsetTime: () => Promise<number>;
    setTimeOffset: (offsetInSeconds: number) => Promise<number>;
    resetTimeOffset: () => Promise<number>;
}
