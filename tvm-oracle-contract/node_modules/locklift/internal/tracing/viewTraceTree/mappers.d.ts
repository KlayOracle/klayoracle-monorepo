import { ViewTraceTree } from "../types";
import BigNumber from "bignumber.js";
import { ContractWithName } from "../../../types";
export declare const extractFeeAndSentValueFromMessage: (traceTree: ViewTraceTree) => {
    totalFees: BigNumber;
    sentValue: BigNumber;
    value: BigNumber;
    balanceChange: BigNumber;
};
export declare const mapParams: (obj: Record<any, any> | Array<any> | undefined, contracts: Array<ContractWithName | undefined>, isFullPrint?: boolean) => Record<any, any>;
