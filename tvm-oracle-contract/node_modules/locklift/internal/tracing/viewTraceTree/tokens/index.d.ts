import { Addressable, ViewTraceTreeWithTotalFee } from "../../types";
export declare class Tokens {
    private readonly viewTraceTree;
    constructor(viewTraceTree: ViewTraceTreeWithTotalFee);
    getTokenBalanceChange: (tokenWallet: Addressable) => string;
}
