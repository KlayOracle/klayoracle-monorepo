import { Addressable, ViewTraceTree } from "../../types";
import BigNumber from "bignumber.js";
export declare const getTokenBalanceChange: (viewTrace: ViewTraceTree, tokenWallet: Addressable) => BigNumber;
